package ru.privatenull.pnauth.dialog;

/** Shared visual dimensions for the built-in authentication dialog on every platform. */
public final class AuthDialogDimensions {
    public static final int FIELD_WIDTH = 200;
    public static final int BODY_WIDTH = 400;
    public static final int SUBMIT_BUTTON_WIDTH = 200;

    private AuthDialogDimensions() {
    }
}
