package ru.privatenull.pnauth.dialog

/** Shared visual dimensions for the built-in authentication dialog on every platform. */
object AuthDialogDimensions {
    const val FIELD_WIDTH: Int = 200
    const val BODY_WIDTH: Int = 400
    const val SUBMIT_BUTTON_WIDTH: Int = 200
}
