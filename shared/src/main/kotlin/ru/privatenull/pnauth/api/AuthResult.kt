package ru.privatenull.pnauth.api

enum class AuthResult {
    SUCCESS,
    ALREADY_REGISTERED,
    USERNAME_TAKEN,
    INVALID_USERNAME,
    NOT_REGISTERED,
    ALREADY_AUTHENTICATED,
    NOT_AUTHENTICATED,
    NOT_JOINED,
    INVALID_PASSWORD,
    PASSWORDS_DO_NOT_MATCH,
    INVALID_PASSWORD_FORMAT,
    TOO_MANY_ATTEMPTS,
    LOCKED_OUT,
    TOTP_REQUIRED,
    TOTP_INVALID,
    TOTP_ALREADY_ENABLED,
    TOTP_NOT_ENABLED,
    TOTP_ENABLED,
    TOTP_DISABLED,
    TOTP_SETUP_REQUIRED,
    RECOVERY_CODE_INVALID,
    PLAYER_NOT_FOUND,
    DIALOG_PREFERENCE_UPDATED,
    DIALOG_PREFERENCE_DISABLED,
    OPERATION_DENIED,
    ADDITIONAL_VERIFICATION_REQUIRED
}
